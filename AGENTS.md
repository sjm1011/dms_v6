# 專案特定開發規則 (Project Custom Rules)

## 批次檔規範 (Batch File Specifications)
* **換行格式**：本專案中的所有 Windows 批次檔（`.bat`、`.cmd`）**必須使用 Windows 的換行符號（CRLF, `\r\n`）**，嚴禁使用 UNIX 的換行符號（LF, `\n`）。
* **編碼規格**：必須以 UTF-8（無 BOM）編碼儲存，且在包含任何中文字元前必須優先執行 `chcp 65001 > nul`。

## SQL 語法
* **格式**：不要寫成一整行，需要寫成易於閱讀的格式

## 系統用字規範

- 系統所有 UI、訊息、參考文件、程式註解與後續新增功能，中文一律以「發佈」為唯一標準用字，不得使用其他異體。

# DMS 輸入畫面統一模式

適用範圍：DMS V6 與後續同類輸入畫面。

## 顯示模式

- 所有輸入畫面統一使用共用 `Modal` 顯示。
- 工具列搜尋框不屬於輸入畫面，不需要改為 `Modal`。

## 鍵盤與焦點行為

- `Modal` 顯示時，focus 必須位於第一個可編輯輸入框。
- 在非最後一個輸入框按下 `Enter`，focus 必須移至下一個可編輯輸入框。
- 僅在最後一個輸入框按下 `Enter` 時，才觸發預設主要按鈕。
- 隱藏、唯讀、停用、核取方塊、選項按鈕及檔案欄位不列入輸入框導航順序。
- 中文輸入法組字期間的 `Enter` 不得觸發欄位導航或提交。

## 含圖示輸入框

- 輸入文字不得與圖示重疊，必須保留足夠的左側內距。
- 圖示必須依輸入框高度垂直置中，使用 `top: 50%` 與 `transform: translateY(-50%)`。
- 圖示設定 `pointer-events: none`，不得攔截輸入框操作。
- 含圖示輸入框在 `Modal` 內不得套用造成中心線偏移的額外上方邊距。

## 驗證

- 修改完成後，從前端專案目錄執行 `npm run build`。
- TypeScript 與 Next.js 正式建置必須成功。

## 參考文件

- 修改「參考文件」資料夾內的 .md 檔後，自動刪除對應的 .html 與 .pdf，我會另外再生成。
- `參考文件/system_specifications.md` 為系統規格的主要依據。系統規格或已實作功能異動時，必須一併檢查並同步更新 `一般使用者說明書.md`、`文件管理員說明書.md` 與 `系統管理員說明書.md`。
- 三本說明書必須使用繁體中文、台灣用語及容易理解的操作語句，避免直接放入資料表、API、程式路徑或其他不影響日常操作的技術內容。
